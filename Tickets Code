# Запрашиваем количество билетов
tickets = int(input("Сколько билетов вы хотите купить? "))

# Объявляем переменные для суммы и скидки
total = 0
discount = 0

# Для каждого билета запрашиваем возраст и определяем стоимость
for i in range(tickets):
    age = int(input(f"Введите возраст посетителя {i+1}: "))
    if age < 18:
        price = 0
    elif 18 <= age <= 25:
        price = 990
    else:
        price = 1390
    # Прибавляем стоимость к общей сумме
    total += price

# Если билетов больше трех, то применяем скидку 10%
if tickets > 3:
    discount = total * 0.1

# Выводим итоговую сумму с учетом скидки
print(f"Сумма к оплате: {total - discount} руб.")
